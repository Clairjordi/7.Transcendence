export enum ChannelStatus {
  Public = "public",
  Private = "private",
  Protected = "protected",
  DirectMessages = "directMessages"
}

export enum UserStatus {
  Online = "online",
  Offline = "offline",
  InGame = "ingame"
}
